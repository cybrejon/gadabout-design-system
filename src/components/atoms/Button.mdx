import { Canvas, Controls, Meta, Primary } from "@storybook/addon-docs/blocks";

import * as ButtonStories from "./Button.stories.svelte";

<Meta of={ButtonStories} />

# Button

A button is a trigger for an action. It contains six variants: Normal, Ghost, Outline, Outline Alternative, Destructive, and Text.
Gadabout's button is designed to look flat, modern, and accessible, without sacrificing visual fidelity.

<Primary />
<Controls />

## Variants

### Normal

The lack of hover feedback is deliberate. The subtle click effect is designed to invoke predictability and reduce cognitive load for the user.

<Canvas of={ButtonStories.Normal} />

### Ghost

Ghost buttons can be overlaid on high tonal elements such as images and solid backgrounds. A ghost button is also used for secondary actions like "Learn More" or "Cancel."

> Note:
> **Use with caution as this tends to fail accessibility tests due to the lack of a solid infill or outline.**

<Canvas of={ButtonStories.Ghost} />

### Outline

Outline buttons exist primarily to provide a medium-emphasis action that separates them from high-emphasis filled buttons and low-emphasis text links.

<Canvas of={ButtonStories.Outline} />

### Outline Alternative

A subtle variation of the outline button with a light background fill, useful for secondary actions that need a bit more visual weight than a ghost button.

<Canvas of={ButtonStories.OutlineAlternative} />

### Destructive

Use the destructive variant for actions that result in data loss or are otherwise irreversible.

<Canvas of={ButtonStories.Destructive} />

### Text

The text variant is used for the lowest emphasis actions, appearing as plain text that reacts on hover.

<Canvas of={ButtonStories.Text} />

## Sizes

### Normal

The default size for buttons, suitable for most use cases.

<Canvas of={ButtonStories.Normal} />

### Small

A more compact version for tight spaces or less prominent actions.

<Canvas of={ButtonStories.Small} />

### Text Sizes

Variants of the text button with different font sizes and spacing.

<Canvas of={ButtonStories.TextMedium} />
<Canvas of={ButtonStories.TextSmall} />

### Sidebar

A full-width variant designed specifically for use in navigation sidebars.

<Canvas of={ButtonStories.Sidebar} />

## Usage Guidelines

### When to use

- Use a button to trigger an immediate action, such as submitting a form, opening a modal, or confirming a selection.
- Use the `normal` variant for the primary action on a page or in a component.

### When not to use

- Do not use a standalone button for navigation to a different page; use the dedicated Sidebar variant or an anchor tag instead.
- Avoid using too many high-emphasis (normal) buttons on a single screen.

### Best practices

- Keep button labels concise and action-oriented (e.g., "Save", "Delete", "Send").
- Ensure the button's variant reflects the priority and nature of the action.
